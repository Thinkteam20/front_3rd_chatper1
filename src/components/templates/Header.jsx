/** @jsx createVNode */
import { createVNode } from "../../lib";

export const Header = () => (
    <header className="bg-blue-600 text-white p-4 sticky top-0 shadow-md z-10">
        <h1 className="text-2xl font-bold tracking-wide">항해플러스</h1>
    </header>
);
